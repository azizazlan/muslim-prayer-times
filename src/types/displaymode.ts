enum DisplayMode {
  DEFAULT = 'DEFAULT',
  PRAYER_TIMES = 'PRAYER_TIMES', // Display prayer timings or schedule for the day
  ADHAN = 'ADHAN',
  IQAMAH = 'IQAMAH',
  SETTINGS = 'SETTINGS',
}

export { DisplayMode };